import { HeroFavoritesContent } from "@/components/hero/HeroFavoritesContent";

export default function FavoritesPage() {
    return (
        <main className="main">
            <HeroFavoritesContent />
        </main>
    );
}
